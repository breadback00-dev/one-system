# Paid Ads Module

Lead source attribution, source-aware nurture sequences, and operator ROI visibility.

## Seeded Proof Pass

Run from repo root:

- `npm run db:push`
- `npm run proof:module4:seeded`

The proof pass seeds one attributed lead, runs one paid nurture campaign, simulates a reply and qualification, records spend, and prints outcome/ROI evidence as JSON.
