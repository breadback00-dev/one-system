# Integrations Package

Typed adapters for Twilio, calendars, CRMs, ads platforms, and review systems.

## Current Capabilities

- Twilio SMS webhook parsing and signature validation.
- Message gateway selection for dev logging or Twilio delivery.
- Module 2 CRM dormant-contact sync contract, including a static dev adapter that maps external records into reactivation import rows.
